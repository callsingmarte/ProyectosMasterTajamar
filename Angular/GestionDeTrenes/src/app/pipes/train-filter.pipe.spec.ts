import { TrainFilterPipe } from './train-filter.pipe';

describe('TrainFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TrainFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
