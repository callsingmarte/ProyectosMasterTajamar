import { TrainNamePipe } from './train-name.pipe';

describe('TrainNamePipe', () => {
  it('create an instance', () => {
    const pipe = new TrainNamePipe();
    expect(pipe).toBeTruthy();
  });
});
