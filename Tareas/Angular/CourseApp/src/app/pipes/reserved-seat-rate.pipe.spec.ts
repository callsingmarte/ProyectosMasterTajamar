import { ReservedSeatRatePipe } from './reserved-seat-rate.pipe';

describe('ReservedSeatRatePipe', () => {
  it('create an instance', () => {
    const pipe = new ReservedSeatRatePipe();
    expect(pipe).toBeTruthy();
  });
});
