import { DiscountAmountDirective } from './discount-amount.directive';

describe('DiscountAmountDirective', () => {
  it('should create an instance', () => {
    const directive = new DiscountAmountDirective();
    expect(directive).toBeTruthy();
  });
});
