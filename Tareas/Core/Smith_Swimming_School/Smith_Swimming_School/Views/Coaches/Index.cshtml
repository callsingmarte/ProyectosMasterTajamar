@model IEnumerable<Smith_Swimming_School.Models.Coach>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Phone_Number)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CoachUser)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Phone_Number)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CoachUser)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_Coach">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_Coach">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_Coach">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
