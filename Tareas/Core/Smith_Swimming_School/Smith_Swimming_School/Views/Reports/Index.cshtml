@model IEnumerable<Smith_Swimming_School.Models.Report>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProgressReport)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Enrollment)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProgressReport)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Enrollment.Id_Enrollment)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_Report">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_Report">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_Report">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
